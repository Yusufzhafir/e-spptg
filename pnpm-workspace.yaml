# Build scripts pnpm is allowed to run. Without approving these, pnpm 11 blocks
# them and the `pnpm install` that runs before scripts (e.g. `pnpm migrate:*`)
# exits with ERR_PNPM_IGNORED_BUILDS.
allowBuilds:
  '@clerk/shared': true
  esbuild: true
  sharp: true
  unrs-resolver: true
